/**
 * Author: wysaid
 * Date: 2023.12.12
 */

import * as vscode from 'vscode';

export async function setupProject(uri?: vscode.Uri): Promise<void> {
    vscode.window.showInformationMessage("EGE: Setup-project not implemented. Do it later...\n");
    console.log("Uri is: ", uri);
}